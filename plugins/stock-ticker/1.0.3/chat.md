# Chat Log — Stock Ticker v1.0.3

> Generated by Plugin Studio on 2026-04-08

---

## You

Currently we see AAPL AAPL for Apple stock. What about to see AAPL and a description instead?

---

## Claude

// @bump: patch

Looking at the code, the `name` field for Finnhub-fetched stocks is set to `symbol` (just the ticker again). I'll fix it so the demo data uses proper company names, and for live Finnhub data I'll fetch the company profile to get a real name — with a fallback to a built-in lookup table.

[Plugin code — see plugin.js]

### What changed

- **Symbol column is now two lines**: ticker on top (`AAPL`), company name below (`Apple Inc.`) — stacked using `flex-direction: column`
- **`NAMES` lookup table** covers 18 common tickers and is used both at render time (initial HTML) and when applying quotes, so the name is always correct
- **Demo data** no longer carries a `name` field — it's all driven by the `NAMES` table now
- **Live Finnhub data** also resolves names from `NAMES` (avoids a costly extra `/profile2` API call per symbol)

---
