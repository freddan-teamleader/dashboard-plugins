# Chat Log — WeConnect Vehicle Tracker v5.0.1

> Generated by Plugin Studio on 2026-04-17

---

## You

Now the widget says that the Agent isn't installed, but the agent says its running

---

## Claude

Let me check what's currently happening with the WeConnect agent and widget setup.The issue is a classic **service dependency mismatch** — the widget is calling `api.getService('weconnect-agent')` at registration time (outside `render()`), but the service plugin may not have fully initialized yet when the widget loads.

Looking at your installed plugins, I can see you have **two** entries for WeConnect:
- **WeConnect Vehicle Tracker** (v5.0.0) — which includes both a service and widget
- **Weconnect Agent** (v3.1.0) — a separate standalone agent

This is likely the root cause — duplicate service registrations can cause `getService` to behave unpredictably.

The fix is to move `api.getService()` **inside** `render()` so it's called after all plugins have initialized. Let me rewrite the widget:

[Plugin code — see plugin.js]

### What changed and why

| Problem | Fix |
|---|---|
| `api.getService()` called **outside** `render()` at module load time — service may not be ready yet | Moved inside `render()` so it runs **after** all plugins are initialized |
| "Agent not installed" even though it's running | `getService` returning `null` too early, now retried fresh on each render |

### Also worth checking

- **Uninstall one of the two WeConnect entries** — having both *WeConnect Vehicle Tracker (v5)* and *Weconnect Agent (v3.1)* registered can cause the service type `weconnect-agent` to be registered twice, which may confuse `getService`. Keep only the standalone **Weconnect Agent** service + this widget.

---
